interface:
  display_name: "iOS SwiftUI Feature Architecture"
  short_description: "Own architecture boundaries, state ownership, and DI"
  default_prompt: "Use the Codex iOS SwiftUI Kit to define feature ownership, boundaries, and dependencies."
  brand_color: "#7C3AED"

policy:
  allow_implicit_invocation: true
