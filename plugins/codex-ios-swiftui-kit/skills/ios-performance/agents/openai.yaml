interface:
  display_name: "iOS SwiftUI Performance Audit"
  short_description: "Audit SwiftUI runtime cost and heavy render paths"
  default_prompt: "Use the Codex iOS SwiftUI Kit to review this SwiftUI view for runtime performance and evidence-based fixes."
  brand_color: "#F97316"

policy:
  allow_implicit_invocation: true
