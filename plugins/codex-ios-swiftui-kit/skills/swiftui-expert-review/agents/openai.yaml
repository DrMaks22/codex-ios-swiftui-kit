interface:
  display_name: "iOS SwiftUI Quality Review"
  short_description: "Run final severity-ranked SwiftUI code review"
  default_prompt: "Use the Codex iOS SwiftUI Kit to review changed SwiftUI files for severity-ranked quality and release readiness."
  brand_color: "#111827"

policy:
  allow_implicit_invocation: true
